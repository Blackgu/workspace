# 2026-07-16 对话要点

## Heartbeat 通知修复
-上午 顾涛：反馈 heartbeat 的完成通知未送达微信主会话。
-上午 David：定位原因为 heartbeat 使用 `sessions_list` 时叠加了不匹配的 `label` 条件，导致无法取得微信主会话的完整 session key。
-上午 顾涛：确认修改方案。
-上午 David：已更新 `HEARTBEAT.md`：按 `openclaw-weixin` 查询并严格匹配当前账号和会话地址，后续归档读取与通知统一使用 `wechatMainSessionKey`；投递失败须明确告警，不得误判为已通知。

## 资讯日报通知验证
-17:08 顾涛：确认暂不调整资讯日报完整性巡检的通知逻辑，明早 08:00–09:00 先观察异常通知是否能送达微信主会话，再决定是否修改。
-晚间 David：执行修改，HEARTBEAT.md 中资讯日报巡检的两类异常通知已统一遵循微信主会话通知规范，使用 wechatMainSessionKey 路由。

## 每日记忆归档未触发 — 根因分析
-23:07 顾涛：发现今日每日记忆归档未触发。
-23:07 David：排查确认 heartbeat 在 20:38–22:38 各轮次均正常触发，但 21:38 轮次中"每日会话清理"检查抢占归档流程，归档步骤被跳过。根因：HEARTBEAT.md 中归档与 21:30–22:00 会话清理同时到期，无强制优先顺序，模型把清理任务优先执行。

## 记忆归档机制重构（独立 cron + heartbeat 兜底）
-23:10 顾涛：确认采用方案 2 + 方案 1。
-23:11 David：
  1. 新增独立归档 cron `daily-conversation-memory-archive`，每 4 小时执行（07:10/11:10/15:10/19:10/23:10），隔离 session，仅做微信主会话对话增量归档。
  2. HEARTBEAT.md 增加「强制执行顺序」章节：归档 → 资讯日报检查 → 会话清理，归档未完成前禁止其他任务抢占或返回 HEARTBEAT_OK。
  3. 独立 cron 已授予 sessions_list、sessions_history、sessions_send 权限，可显式定位和读取微信主会话。
- cron 创建后即手动触发验证，当前 cron session 即为验证运行。
