# 2026-07-02 对话要点

## 记忆管理
- 顾涛询问记忆晋升机制，确认当前为手动模式：memory/YYYY-MM-DD.md 由 HEARTBEAT 半自动创建，MEMORY.md 由 David 手动筛选写入，无自动晋升
- 顾涛选择方案B（半自动晋升）：每周日 Cron 生成候选清单推微信，顾涛确认后写入 MEMORY.md
- 创建 `weekly-memory-promotion` cron 任务（每周日20:00），模型 `glm/glm-5.2`，fallback `deepseek-v4-pro`，isolated + thinking off
- 顾涛拒绝将 7/1 日志中的 cron 决策补入 MEMORY.md

## 系统运维
- 微信消息响应慢排查：Gateway 日志显示消息正常收发，问题在微信客户端刷新
- 排查 main agent 多个 session 来源：通道隔离（weixin/webchat）、cron 任务、持久化 session（世界杯）
- 世界杯战报 cron：sessionTarget 从 `session:worldcup-reporter` 改为 `isolated`，thinking 从 `medium` 改为 `off`
- 清理 sessions.json 中两个旧 default weixin session stub（9天前残留）
- Heartbeat 排查：`target: "last"` 导致心跳投递到 webchat session 而非微信 session，memory 日志未生成
- Heartbeat 配置修复：添加 `to` 绑定微信 openID，后尝试 `isolatedSession: true` 发现导致无法读取对话历史，回退为不设置 isolatedSession
- HEARTBEAT.md 时间窗口优化方案讨论，最终回滚保持原版
- 手动触发 HEARTBEAT 巡检，创建本日志文件

## 记账
- 补记 6/24：1628储蓄卡 支出 ¥15,500 购物（徕卡镜头）
- 拉取 1628储蓄卡全部账单，结余 ¥225,148.76（收入 ¥316,795.76 - 支出 ¥91,647.00）

## 配置变更记录
- 所有 cron 任务配置统一：isolated + thinking off
- Heartbeat 配置：`to` 绑定微信 openID，未启用 isolatedSession
- 世界杯 cron 模型仍为 `deepseek-v4-pro`（未变更）
