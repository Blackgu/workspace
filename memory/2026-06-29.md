# 2026-06-29 日记

## 系统运维
- **Cron 故障排查**：7:30 二轮采集 Cron 因 `LLM request failed`（DeepSeek API 掉线）和 `Apply Patch failed`（toolsAllow 缺失）连续失败两次，手动补跑后简报已完整
- **Cron 配置修复**：
  - Round 2 的 toolsAllow 增加 `apply_patch`
  - 两个简报 Job 模型从 deepseek-v4-pro 切到 deepseek-v4-flash（节省成本）
  - 两个简报 Job 增加 failureAlert（失败后直接微信通知，after=1, cooldown=1h）
- **Heartbeat 问题**：确认 heartbeat 依赖模型自觉执行 HEARTBEAT.md 检查项，不可靠；已通过 Cron 自身的 failureAlert 弥补

## 信息查询
- 杭州天气：6月29日多云转阵雨，24°C~31°C，闷热有雨，紫外线指数10

## 待跟进
- 每日对话日志归档仍依赖 heartbeat 自动生成，可靠性不足，需考虑更可靠的方案
