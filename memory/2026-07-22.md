# 2026-07-22 日志

**上次归档时间：** 2026-07-22 11:10

## 技术方案与决策
- 07:16 顾涛：询问如何为 OpenClaw 接入视觉模型服务，以便微信发图时能识别图片内容。
- 07:17 David：分析现有配置，发现未配置 `agents.defaults.imageModel`，建议接入阿里云百炼 DashScope Qwen-VL（`qwen/qwen-vl-max-latest`），改动最小（已有 DASHSCOPE_API_KEY）。备选方案：使用现有 HotAI 供应商的视觉模型（如 GPT-4o、Gemini、Claude 视觉版），需确认其可用的视觉模型 ID。该配置为全局变更，需顾涛确认后执行。

## 视觉模型配置演进
- 11:03 顾涛：指定视觉模型使用北京 MaaS 兼容接口（baseUrl: `https://llm-56q37y6t6hlzqjzi.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`），模型 `qwen3.7-plus`。
- 11:04 David：通过 jq 直写 `openclaw.json` 新增 `aliyun-maas` Provider，将 `aliyun-maas/qwen3.7-plus` 设为 `agents.defaults.imageModel.primary`，配置校验通过。
- 11:05 测试：`image` 工具报 `Unknown model: aliyun-maas/qwen3.7-plus`，说明 OpenClaw 的图片工具不支持自定义 Provider 路由。
- 11:06 顾涛：将模型改为 `qwen3-vl-plus`。David 更新配置，`image` 工具仍报 Unknown model。
- 11:09 顾涛：要求再试图片识别，仍失败。David 解释需改用内置 `qwen` Provider（而非自定义 `aliyun-maas`），其他配置不变。
- 11:10 顾涛确认。David 将 Provider 改为内置 `qwen`（baseUrl 和密钥不变），改为 `qwen/qwen3-vl-plus`，配置校验通过。
- 11:11 复测：`image` 工具仍报 `Unknown model: qwen/qwen3-vl-plus`。David 定位原因：当前 OpenClaw Qwen 媒体理解模块仅注册 `qwen-vl-max-latest`，未注册 `qwen3-vl-plus`。建议改回 `qwen/qwen-vl-max-latest`，待顾涛确认。

## 待办事项
- 顾涛需确认是否改用 `qwen/qwen-vl-max-latest` 作为视觉模型。
